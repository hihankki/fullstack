services:
  backend:
    build: ./backend
    container_name: comments-backend
    ports:
      - "8000:8000"
    restart: always

  frontend:
    build: ./frontend
    container_name: comments-frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
    restart: always